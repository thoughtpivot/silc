// AUTO-GENERATED BY silc — ShadCN-style RadioGroup primitive. DO NOT EDIT
import * as React from "react";
import { cn } from "../../lib/utils";
import { Label } from "./label";

export interface RadioGroupProps {
  id?: string;
  label?: string;
  name: string;
  value: string;
  options: string[];
  onChange: (value: string) => void;
  disabled?: boolean;
  className?: string;
}

export function RadioGroup({
  id,
  label,
  name,
  value,
  options,
  onChange,
  disabled = false,
  className,
}: RadioGroupProps) {
  return (
    <div className={cn("grid gap-2", className)}>
      {label ? <Label htmlFor={id || name}>{label}</Label> : null}
      <div role="radiogroup" aria-label={label || name} className="flex flex-wrap gap-3">
        {options.map((option) => {
          const optionId = `${name}-${option.replace(/\s+/g, "-").toLowerCase()}`;
          const selected = value === option;
          return (
            <label
              key={option}
              htmlFor={optionId}
              className={cn(
                "inline-flex cursor-pointer items-center gap-2 rounded-full border px-3 py-1.5 text-sm transition",
                disabled && "cursor-not-allowed opacity-50",
                selected
                  ? "border-[var(--silc-accent)] bg-primary/15 text-foreground"
                  : "border-border bg-background/50 text-muted-foreground hover:border-ring"
              )}
            >
              <input
                id={optionId}
                type="radio"
                name={name}
                value={option}
                checked={selected}
                disabled={disabled}
                onChange={() => onChange(option)}
                className="accent-[var(--silc-accent)]"
              />
              {option}
            </label>
          );
        })}
      </div>
    </div>
  );
}
