// AUTO-GENERATED BY silc — inventory search primitive. DO NOT EDIT
import * as React from "react";
import { Input } from "./input";
import { Label } from "./label";

export function SearchInput({
  id,
  value,
  onChange,
  onAiSearch,
  label = "Search products",
  placeholder = "Name, category, aisle, tag, or natural language…",
  searching = false,
}: {
  id: string;
  value: string;
  onChange: (value: string) => void;
  onAiSearch: () => void;
  label?: string;
  placeholder?: string;
  searching?: boolean;
}) {
  return (
    <div className="grid min-w-0 flex-1 gap-2">
      <Label htmlFor={id}>{label}</Label>
      <div className="flex gap-2">
        <Input
          id={id}
          value={value}
          placeholder={placeholder}
          onChange={(event) => onChange(event.target.value)}
        />
        <button
          type="button"
          disabled={searching || !value.trim()}
          onClick={onAiSearch}
          className="rounded-lg bg-primary px-4 text-sm font-medium text-primary-foreground disabled:opacity-50"
        >
          {searching ? "Interpreting…" : "AI filter"}
        </button>
      </div>
    </div>
  );
}
