// AUTO-GENERATED BY silc — OpenTUI terminal entry. DO NOT EDIT
// TerminalApp.tsx uses classic JSX → OpenTUI `h` (not React). Bun must be
// launched with: --jsx-runtime=classic --jsx-factory=h --jsx-fragment=Fragment
import { mountTerminalApp } from "./src/components/terminal/runtime";
import { App } from "./src/TerminalApp";

const mode = process.env.SILC_TERMINAL_MODE || "opentui";
if (mode === "opentui" || mode === "auto") {
  mountTerminalApp(App).catch((error) => {
    console.error("silc OpenTUI terminal failed:", error);
    process.exit(1);
  });
}
