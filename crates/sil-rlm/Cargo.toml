[package]
name = "sil-rlm"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Closed-tool recursive language model scaffold for silc assist (ADR-008)"

[dependencies]
sil-training = { workspace = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"

[dev-dependencies]
tempfile = "3"
