[package]
name = "sil-router"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Silc semantic router (Go / Python / Bun-TypeScript) — stub"

[dependencies]
sil-core = { workspace = true }

[dev-dependencies]
sil-parser = { workspace = true }
