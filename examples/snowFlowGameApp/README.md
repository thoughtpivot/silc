# snowFlowGameApp

Web-only Silc `game::` example (ADR-012). Author surface is `main.silc` + `AGENTS.md`;
the Babylon/WebGPU runtime is compiler-owned.

```bash
silc main.silc
```

Serves on http://127.0.0.1:18140. Requires WebGPU. No `--terminal` surface.
