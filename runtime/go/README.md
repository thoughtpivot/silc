# Go Runtime Templates

Future compiler-shipped harness templates for SIL modules routed to Go.

This directory is part of the compiler scaffold. User-program output is
generated under `{workdir}/.runtime/go/`, not here.
